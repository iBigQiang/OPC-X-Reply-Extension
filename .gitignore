# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# IDE / 编辑器
.vscode/
.idea/
*.swp
*.swo
*~

# Node（项目本身无 npm 依赖，但留个口子）
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 调试输出
*.log

# Claude / 本地规划草稿（仅本机使用，不入库）
.claude/

# 方案调研报告 / 迭代需求草稿（仅本机使用，不入库）
# 历史教训：2026-05-18 曾把含真实 DeepSeek API Key 的迭代需求草稿误推到 GitHub
# 被 GitGuardian 检出。此目录从此不进仓库，避免类似事故重演。
docs/开发及迭代方案调研报告/

# 密钥 / 凭据相关防御性规则（即使没在用也提前挡住）
*.env
.env.*
secrets.*
*.secret
*.key
*.pem
*credentials*
*api_key*
*apikey*
